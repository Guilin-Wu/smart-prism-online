# 项目文件结构说明

```
Smart-Prism/
├── index.html                 # 主HTML文件
├── README.md                  # 项目说明
├── STRUCTURE.md              # 本文件 - 结构说明
│
├── assets/                    # 静态资源
│   └── images/
│       └── logo.png          # Logo图片
│
├── css/                       # 样式文件
│   ├── main.css              # 主样式入口（导入其他CSS）
│   ├── variables.css         # CSS变量定义
│   ├── base.css              # 基础样式（待拆分）
│   ├── sidebar.css           # 侧边栏样式（待拆分）
│   ├── modules.css           # 模块样式（待拆分）
│   ├── charts.css            # 图表样式（待拆分）
│   ├── print.css             # 打印样式（待拆分）
│   └── themes.css            # 主题样式（待拆分）
│
├── js/                        # JavaScript文件
│   ├── main.js               # 主入口文件（待创建）
│   │
│   ├── config/               # 配置文件
│   │   ├── config.js         # 全局配置
│   │   ├── constants.js      # 常量定义
│   │   └── state.js          # 全局状态管理
│   │
│   ├── api/                  # API相关（为服务器连接预留）
│   │   └── api.js            # API客户端
│   │
│   ├── auth/                 # 认证相关（为登录功能预留）
│   │   └── auth.js           # 认证管理
│   │
│   ├── data/                 # 数据管理（待拆分）
│   │   ├── storage.js        # 本地存储管理
│   │   └── parser.js        # Excel解析
│   │
│   ├── utils/                # 工具函数（待拆分）
│   │   ├── statistics.js     # 统计计算
│   │   ├── charts.js         # 图表工具
│   │   └── helpers.js        # 辅助函数
│   │
│   ├── modules/              # 功能模块（待拆分）
│   │   ├── dashboard.js      # 整体成绩分析
│   │   ├── student.js        # 学生个体报告
│   │   ├── paper.js          # 试卷科目分析
│   │   ├── single-subject.js # 单科成绩分析
│   │   ├── boundary.js       # 临界生分析
│   │   ├── holistic.js       # 全科均衡分析
│   │   ├── trend.js          # 成绩趋势对比
│   │   ├── groups.js         # 学生分层筛选
│   │   ├── correlation.js    # 学科关联矩阵
│   │   ├── weakness.js       # 偏科诊断分析
│   │   ├── item-analysis.js  # 学科小题分析
│   │   ├── ai-advisor.js     # AI智能分析
│   │   ├── goal-setting.js   # 目标与规划
│   │   ├── exam-arrangement.js # 考场编排
│   │   ├── study-groups.js   # 智能互助分组
│   │   ├── comment-gen.js    # 评语生成助手
│   │   ├── weakness-workbook.js # 错题攻坚本
│   │   └── multi-exam.js     # 数据管理中心
│   │
│   └── charts/                # ECharts图表函数（待拆分）
│       ├── common.js         # 通用图表
│       ├── dashboard.js      # Dashboard图表
│       └── student.js        # 学生相关图表
│
└── server/                    # 后端服务器（未来添加）
    ├── server.js             # 服务器入口
    ├── routes/               # 路由
    │   ├── auth.js           # 认证路由
    │   └── data.js           # 数据路由
    └── models/               # 数据模型
        └── user.js           # 用户模型
```

## 🔄 迁移计划

### 阶段一：基础结构（已完成 ✅）
- [x] 创建目录结构
- [x] 创建配置文件（config.js, constants.js, state.js）
- [x] 创建API和认证模块框架
- [x] 创建CSS基础结构
- [x] 移动logo到assets目录

### 阶段二：CSS拆分（进行中）
需要将 `style.css` (1809行) 拆分为：
1. `base.css` - 基础样式（body, h1-h6, 通用类）
2. `sidebar.css` - 侧边栏相关样式
3. `modules.css` - 各模块特定样式
4. `charts.css` - 图表容器样式
5. `print.css` - 打印相关样式
6. `themes.css` - 主题切换样式

### 阶段三：JavaScript拆分（待进行）
需要将 `script.js` (22019行) 拆分为：

#### 3.1 数据层
- `data/storage.js` - 本地存储操作（localforage相关）
- `data/parser.js` - Excel文件解析

#### 3.2 工具层
- `utils/statistics.js` - 统计计算函数
- `utils/charts.js` - 图表辅助函数
- `utils/helpers.js` - 通用工具函数

#### 3.3 模块层
每个功能模块一个文件，包含：
- 模块的渲染函数
- 模块的事件处理
- 模块的特定逻辑

#### 3.4 图表层
- `charts/common.js` - 通用图表函数
- `charts/dashboard.js` - Dashboard相关图表
- `charts/student.js` - 学生相关图表

#### 3.5 主入口
- `main.js` - 应用初始化、事件绑定、路由

## 🚀 如何使用新结构

### 当前状态
- 项目仍使用旧的 `script.js` 和 `style.css`
- 新的结构已创建，但代码尚未迁移
- 可以逐步迁移，不影响现有功能

### 下一步操作

1. **更新HTML引用**（已准备）
   - 将 `style.css` 改为 `css/main.css`
   - 将 `script.js` 改为使用模块化导入（需要构建工具或使用ES6模块）

2. **逐步迁移代码**
   - 建议先迁移CSS（相对独立）
   - 再迁移工具函数（依赖较少）
   - 最后迁移模块代码

3. **添加构建工具**（可选）
   - 使用 Webpack/Vite 打包模块
   - 或使用原生ES6模块（需要服务器环境）

## 🔐 服务器和登录功能集成

### API配置
在 `js/config/config.js` 中配置服务器地址：
```javascript
API: {
    baseURL: 'http://your-server.com', // 设置服务器地址
    // ...
}
```

### 认证流程
1. 用户登录 → `authManager.login()`
2. 获取token → 存储在localStorage
3. API请求 → 自动添加Authorization头
4. 登出 → `authManager.logout()`

### 数据同步
- 本地优先：先使用localforage存储
- 后台同步：通过API同步到服务器
- 冲突处理：服务器数据优先

## 📝 注意事项

1. **兼容性**
   - ES6模块需要现代浏览器或构建工具
   - 建议使用构建工具（如Vite）进行开发

2. **渐进式迁移**
   - 可以保持旧代码运行
   - 逐步迁移模块，不影响现有功能

3. **测试**
   - 每个模块迁移后都要测试
   - 确保功能完整性

## 🛠️ 推荐工具

- **构建工具**: Vite (推荐) 或 Webpack
- **代码格式化**: Prettier
- **代码检查**: ESLint
- **版本控制**: Git

